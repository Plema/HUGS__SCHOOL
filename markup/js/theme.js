var site_url = document.location.origin

jQuery(document).ready(function ($) {
  $('.menu-list li a').eq(0).removeClass('anchor')

  //c	var menu = $(".menu-list").html();
  //c	$('#test').text(menu);
}) //endready
